# Wandas 0.6 persistence boundaries

- Status: Accepted
- Date: 2026-07-16

## Decision

WDF and Recipe JSON are independent artifacts with different responsibilities.

### WDF 0.3

WDF stores one concrete analysis result. Version 0.3 writes the complete Frame tensor
once at `/data`, channel metadata separately under `/channels`, represented dimension
coordinates under `/coordinates`, and schema-versioned Frame state in root attributes.
An explicit codec registry covers `ChannelFrame`, `SpectralFrame`, `SpectrogramFrame`,
`CepstralFrame`, `CepstrogramFrame`, `NOctFrame`, and `RoughnessFrame`.

The codec state contains the exact Frame type, semantic dimensions, and the constructor
parameters needed to reconstruct its analysis domain. Loading validates all three
before returning a typed Frame. WDF 0.1 and 0.2 remain readable as `ChannelFrame`; their
stored `frame_type` string was not a complete reconstruction contract and is not used
to guess a type.

WDF retains display `operation_history` as a source prefix. It does not restore live
semantic lineage, operation objects, or a Dask graph.

### Recipe schema 2 JSON

`RecipePlan.save()` and `RecipePlan.load()` persist reusable operation intent in a
standalone `.recipe.json` file. The artifact contains named inputs and the portable
operation graph, but never Frame samples or Dask tasks. It uses the existing strict
`wandas.recipe` schema version 2 and refuses unknown schema versions or malformed JSON.

A typical evidence pair is:

```text
analysis.wdf
analysis.recipe.json
```

The first is a concrete typed result; the second is a reusable processing plan. WDF
does not embed or reference Recipe JSON in 0.6, keeping their compatibility schedules
independent. Analyzer can consume either artifact without a Wandas-specific GUI API.

## Compatibility rules

- Readers accept WDF 0.1, 0.2, and 0.3; unsupported future versions fail explicitly.
- WDF 0.3 Frame-state schema 1 and Recipe schema 2 require exact fields.
- Runtime-only or unregistered operations fail Recipe extraction; they are not silently
  converted to display history.
- Saving WDF materializes Frame data. Streaming/chunked writes are deferred until an
  implementation can preserve typed state and signal-processing correctness.

## 日本語要約

WDF は型付き解析結果、`.recipe.json` は再利用可能な処理手順です。WDF 0.3 は全 built-in Frame の型・軸・解析パラメータを復元し、旧 0.1/0.2 は ChannelFrame として読みます。Recipe は WDF に埋め込まず、両 schema の互換性を独立させます。
