# Datasets Module / データセットモジュール

The `wandas.datasets` module contains sample-data helpers for tests and
demonstrations.

`wandas.datasets`はテストとデモで使うサンプルデータのhelperを提供します。

::: wandas.datasets.sample_data
