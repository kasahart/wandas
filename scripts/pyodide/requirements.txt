# Constraints for pure-Python dependencies that are not part of the Pyodide
# 314.0.3 lock. The harness passes these through micropip's constraints option
# so the Wandas wheel remains the only top-level install requirement.
cattrs==26.1.0
locket==1.0.0
partd==1.4.2
